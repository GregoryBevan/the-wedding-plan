info:
  name: Search Deezer
  type: http
  seq: 4

http:
  method: GET
  url: https://api.deezer.com/search?q=kamo
  params:
    - name: q
      value: kamo
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
