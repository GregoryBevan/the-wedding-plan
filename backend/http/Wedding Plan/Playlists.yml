info:
  name: Playlists
  type: http
  seq: 7

http:
  method: GET
  url: https://api.deezer.com/user/me/playlists?access_token={{accessToken}}
  params:
    - name: access_token
      value: "{{accessToken}}"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
