services:
  db:
    image: postgres:18-alpine
    container_name: wedding-plan-db
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: wedding_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
